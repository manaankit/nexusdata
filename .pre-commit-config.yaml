repos:
  - repo: local
    hooks:
      - id: dq-rule-validation
        name: Validate DQ rule assets
        entry: python3 scripts/validate_rules.py --strict-references
        language: system
        pass_filenames: false
